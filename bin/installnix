#!/bin/bash

# sh <(curl -L https://nixos.org/nix/install) \
#           --darwin-use-unencrypted-nix-store-volume \
#           --daemon

sh <(curl -k -L https://nixos.org/nix/install) \
          --darwin-use-unencrypted-nix-store-volume
